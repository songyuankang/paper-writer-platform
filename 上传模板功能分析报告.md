# 上传模板功能分析报告

> 生成时间：2026-08-11 ｜ 只分析，未修改任何代码
> 适用分支：`agent/template-management-ui`（当前工作区）

---

## 0. 核心结论（TL;DR）

1. **上传链路完整可用**：前端选文件 → `POST /api/format/templates` → 后端校验 → DOCX 解析 → 生成 v2「我的模板」→ 入库 SQLite → 可查看/编辑/套用。
2. **存在两代上传机制**：
   - **v2（现行）**：上传 → 后端解析 → 结构化字段写入 v2 模板（`format_templates.content`）。
   - **v1（遗留死代码）**：前端 `UploadTemplate.tsx` + `templateStore.ts`（IndexedDB 本地存 .docx），**当前没有任何页面引用**；后端 v1 `create_template()` 也**无调用方**，仅测试使用。磁盘上残留 6 个旧上传模板目录（DB 中标记 `legacy=1`）。
3. **最大缺口**：DOCX 解析器（`parse_template.py`）能力很强，能提取页边距/字体/字号/标题样式/页眉页脚/目录/参考文献/封面/兼容度评分，但**上传接口只把「页边距 + 页眉页脚距」写入了 v2 模板**，其余解析结果全部被丢弃（仅兼容度评分被写进 `description`）。字体、字号、标题格式、页眉页脚、目录、参考文献均**未**落进模板数据。
4. **原始 .docx 不落盘**：上传解析后临时文件即被删除，v2 模板不保存原始文件（v1 才存 style.docx），因此上传模板无法用于「按原模板排版」，只能作为「样式参考生成新模板」。

---

## 1. 前端定位

### 1.1 上传入口（3 处，均为 v2 上传）

| 入口 | 文件 | 位置 | 说明 |
|---|---|---|---|
| 模板管理页「上传模板」 | `paper-writer-web/src/pages/Templates.tsx` | `handleUpload`（约 L199） | 上传后自动打开详情 |
| 格式处理页「上传模板」 | `paper-writer-web/src/pages/Format.tsx` | 约 L123 | 上传并直接选用 |
| 弹窗式模板管理「上传 DOCX」 | `paper-writer-web/src/components/TemplateManagerModal.tsx` | 约 L346 | 弹窗内上传 |

三个入口调用同一个 API 函数 `uploadFormatTemplate`（`src/api/paper.ts` L983）。

### 1.2 上传逻辑（以 Templates.tsx 为例）

```
handleUpload(file)
 ├─ 校验后缀 .docx
 ├─ uploadFormatTemplate({ name, school_name, major, paper_type }, file)
 │    └─ new FormData()：name / school_name / major / paper_type / file
 │         → POST /api/format/templates
 ├─ 成功：load() 刷新列表 + openDetail(uploaded.id) 打开详情预览
 └─ 失败：setError 提示
```

### 1.3 上传后的数据保存位置

- **v2（现行）**：保存到**后端** SQLite `format_templates` 表（`type='mine'`，`content` 列存完整模板 JSON）。前端不落盘。
- **v1（遗留死代码）**：`utils/templateStore.ts` 用 **IndexedDB**（数据库名 `paper-writer-templates`，对象仓库 `templates`，keyPath `id`）把整个 .docx 存浏览器本地，另有 localStorage key `paper-writer-last-template` 记录最近选用。**该组件已无引用**，属于历史遗留。

### 1.4 模板预览

- 整页管理：`Templates.tsx` 左侧列表 + 右侧 `TemplateEditor.tsx` 全字段编辑。
- 弹窗管理：`TemplateManagerModal.tsx` 左侧分类列表 + 右侧 13 个分区编辑（复用 `StyleFieldEditor.tsx`）。
- 两者共用同一套 API（`GET /api/templates`、`GET /api/templates/{id}`、`PUT /api/templates/{id}`）。

---

## 2. 后端定位

### 2.1 接收上传文件的 API

`app/api/format_task.py` → `POST /api/format/templates`

- 表单字段：`name`（必填）、`school_name`、`major`、`paper_type`、`file`（UploadFile）
- 校验链：
  1. 文件大小 ≤ `settings.max_upload_bytes`（20 MB，超限 413）
  2. 后缀必须 `.docx`（否则 400）
  3. 魔数 `PK\x03\x04` 且包含 `word/document.xml`（校验是否为真实 Word 文档，否则 400）

### 2.2 文件保存位置

**v2 流程不保存原始文件**：写入临时文件 → 解析 → `tmp_path.unlink()` 删除。

- 旧版 v1 目录结构（`app/formatter/template_manager.py`，已无调用方）：
  ```
  formatter/templates/<uuid>/
    ├── style.docx              上传的原始 Word 模板
    ├── template.json           元数据
    ├── rules.json              格式规则（目录/页码/标题编号/参考文献/图表）
    └── template_config.json    自动解析结果
  ```

### 2.3 DOCX 解析逻辑

`paper_writer_scripts/parse_template.py`（python-docx），核心函数 `parse_document()`，输出 profile：

| 模块 | 提取内容 |
|---|---|
| `page`（`page_info()`） | 纸张宽高、上下左右页边距、页眉/页脚距、装订线 gutter、页码格式/起始、页眉页脚是否含内容、首页不同 |
| `fonts`（`doc_defaults()`） | 文档默认字体（ascii / eastAsia / hAnsi）、默认字号、默认行距/段前段后/首行缩进 |
| `styles`（按角色找样式） | heading1/2/3、body、caption、reference、toc_entries、table：字体（name/ascii/eastAsia/size/bold/color）+ 段落（alignment/line_spacing/space_before/space_after/first_line_indent/outline_level） |
| `cover` | 封面段落检测 + 字段识别（论文题目/学校/学院/专业/班级/姓名/学号/指导教师/日期/年级） |
| `toc` | TOC 域检测（instrText/fldSimple）+ 目录条目样式名 |
| `structure` | 逐段分类（cover/toc/heading/body）、是否含参考文献章节 |
| `compatibility` | 14 项兼容度评分（满分 100） |

### 2.4 模板生成逻辑

`format_task.py::upload_template`：

```python
config = template_manager._extract_config(tmp_path)   # 调用 parse_template.parse_document()
page = config.get("page", {})
data = {
    "name": name, "school_name": ..., "major": ..., "paper_type": ...,
    "description": "上传 DOCX 自动分析：兼容性评分 X/100",
    "page": {
        "size": "A4", "orientation": "portrait",
        "margins": {top/bottom/left/right_mm},        # cm × 10 → mm
        "header_distance_mm": ..., "footer_distance_mm": ...,
    },
}
template = get_template_service().create_from_data(data)
```

`TemplateService.create_from_data()`（`formatter/template/service.py` L146）：
基于默认模板 deepcopy → `_apply_template_data` 合并字段 → `TemplateValidator` 校验 → `TemplateRepository.create()` → INSERT 到 SQLite。

### 2.5 数据库存储

`app/db.py` 定义 `format_templates` 表（19+ 列，含迁移兼容逻辑）：

| 列 | 说明 |
|---|---|
| `id` | 主键（uuid hex） |
| `name` / `school_name` / `major` / `paper_type` | 模板元信息 |
| `type` | `mine`（我的）/ `basic` / `school` |
| `category` / `description` / `version` / `schema_version` | 分类、描述（含兼容度评分）、版本 |
| `source` | `db`（我的）/ 内置为文件路径 |
| `content` | **完整模板 JSON（v2 核心）** |
| `parent_id` | 复制溯源 |
| `is_favorite` / `is_default` / `sort_order` | 收藏 / 默认 / 排序 |
| `dir` | v1 遗留：模板目录路径 |
| `legacy` | 1 = 旧上传模板（有 dir 无 content），保留原数据待迁移 |
| `created_at` / `updated_at` | 时间戳 |

> 内置模板（basic/school）存文件 `formatter/templates/<dir>/template.json`，DB 行仅存状态；我的模板存 DB。

---

## 3. 完整流程

```
用户选择 .docx
      │
      ▼
前端组件（Templates.tsx / Format.tsx / TemplateManagerModal.tsx）
      │  handleUpload → uploadFormatTemplate() → FormData(name, school_name, major, paper_type, file)
      ▼
API 接口  POST /api/format/templates   (format_task.py)
      │  ① 大小校验 ≤20MB  ② 后缀 .docx  ③ 魔数 PK\x03\x04 + word/document.xml
      ▼
后端处理（临时文件）
      │  template_manager._extract_config(tmp)  →  parse_template.parse_document()
      ▼
解析内容（profile JSON）
      │  page / fonts / styles(标题·正文·题注·参考文献·表格·目录) / cover / toc / structure / compatibility
      ▼
生成模板数据（仅消费 page 子集）
      │  margins(mm) + header/footer_distance → create_from_data() → 基于默认模板生成
      ▼
保存位置  SQLite format_templates 表（type=mine, content=完整模板 JSON）
      │  （临时 docx 已删除，原始文件不落盘）
      ▼
前端刷新列表 + openDetail 预览/编辑（GET /api/templates/{id}）
```

---

## 4. 重点检查：可提取字段（按你列出的清单逐项核对）

| 字段 | 解析器是否提取 | **是否写入 v2 模板** | 说明 |
|---|:---:|:---:|---|
| 页边距 | ✅ `page.margins`（cm→mm） | ✅ | **唯一完整写入的字段** |
| 页眉/页脚距 | ✅ `header/footer_distance_cm` | ✅ | 同上 |
| 字体 | ✅ docDefaults + 各样式字体 | ❌ | 解析后丢弃 |
| 字号 | ✅ 各样式 size_pt | ❌ | 解析后丢弃 |
| 标题格式 | ✅ heading1/2/3 完整样式 | ❌ | 解析后丢弃 |
| 页眉页脚内容 | ✅ has_content / different_first_page | ❌ | 解析后丢弃（v2 模板只有距离） |
| 目录 | ✅ TOC 域 + 目录条目样式名 | ❌ | 解析后丢弃 |
| 参考文献 | ✅ reference 样式 + has_references | ❌ | 解析后丢弃 |
| 封面 | ✅ 字段识别（题目/学校/学号等） | ❌ | 解析后丢弃 |
| 兼容度评分 | ✅ compute_compatibility（100 分） | ⚠️ 仅写入 description | 以文本形式出现在描述里 |

**结论：解析器具备全部字段的提取能力，但上传接口只消费了 `page` 的页边距/页眉页脚距，其余能力闲置。** 如果要让「上传模板 → 自动生成完整样式模板」真正成立，需要在 `format_task.py::upload_template` 里把 `config["styles"]`、`config["fonts"]`、`config["toc"]`、`config["cover"]` 等映射进 `data`（`TemplateService._apply_template_data` 已支持 styles/blocks 等结构化写入，改造点集中、可复用）。

---

## 5. 当前模板数据格式

### 5.1 DOCX 解析输出 JSON（parse_template.py 的 profile 结构）

```json
{
  "source": "school_template.docx",
  "page": {
    "page_width_cm": 21.0, "page_height_cm": 29.7,
    "top_margin_cm": 2.54, "bottom_margin_cm": 2.54,
    "left_margin_cm": 3.18, "right_margin_cm": 3.18,
    "header_distance_cm": 1.5, "footer_distance_cm": 1.75,
    "page_number_format": "decimal", "page_number_start": null,
    "gutter_cm": 0.0,
    "header": {"has_content": true, "different_first_page": true},
    "footer": {"has_content": true, "different_first_page": false}
  },
  "fonts": {
    "default": {"ascii": "Times New Roman", "east_asia": "宋体",
                "h_ansi": "Times New Roman", "size_pt": 12}
  },
  "styles": {
    "heading1": {
      "name": "标题 1",
      "font": {"name": "标题 1", "ascii": "Times New Roman",
               "east_asia": "黑体", "size_pt": 16, "bold": true},
      "paragraph": {"alignment": "CENTER", "line_spacing": 1.5,
                    "space_before_pt": 0, "space_after_pt": 0,
                    "first_line_indent_pt": null, "outline_level": 0}
    },
    "heading2": null, "heading3": null,
    "body": {"name": "正文", "font": {...}, "paragraph": {...}},
    "caption": null, "reference": null,
    "toc_entries": ["目录 1", "目录 2"],
    "table": {"name": "Table Grid"}
  },
  "cover": {"detected": true, "fields": [
    {"keyword": "论文题目", "key": "title", "paragraph_index": 2, "text": "论文题目：..."}
  ]},
  "toc": {"detected": true, "field_paragraphs": [18]},
  "structure": {"items": [...], "has_references": true},
  "compatibility": {"score": 92, "max": 100, "missing": []}
}
```

### 5.2 数据库表结构（SQLite `format_templates`）

见上文 2.5 表格；v2 上传行示例：`id=uuid, type='mine', source='db', schema_version=2, content=<完整模板JSON>, legacy=0`。

### 5.3 前端 TypeScript interface（`src/api/paper.ts`）

上传请求相关：

```ts
export interface FormatTemplate {          // GET /api/format/templates 返回项
  id: string; name: string; school_name: string;
  major: string; paper_type: string; type: string; source: string;
  description: string;
}

export async function uploadFormatTemplate(
  meta: { name: string; school_name: string; major: string; paper_type: string },
  file: File,
): Promise<FormatTemplate>;
```

模板详情相关（v2 核心类型）：

```ts
export interface TemplateSummary { id; name; description; category; paper_type;
  source; type; school_name; major; version; created_at; updated_at;
  is_default; is_favorite; has_cover; legacy; editable; }

export interface TemplateStyleInput {
  font_family: { east_asia: string; latin: string };
  font_size_pt: number; bold: boolean; italic: boolean; underline: boolean;
  alignment: TemplateAlignment;
  line_spacing: { mode: TemplateLineSpacingMode; value: number };
  space_before_pt: number; space_after_pt: number;
  first_line_indent: { unit: TemplateIndentUnit; value: number };
  keep_with_next: boolean; page_break_before: boolean;
}

export interface TemplateBlockInput { key; kind; label; enabled; level?;
  styles: Record<string, TemplateStyleInput>; settings: Record<string, unknown>; }

export interface TemplatePageInput {
  size: TemplatePageSize; orientation: TemplateOrientation;
  margins: { top_mm; bottom_mm; left_mm; right_mm };
  header_distance_mm?; footer_distance_mm?;
}

export interface TemplateWritePayload { base_template_id?; name; description; ... }
```

> v1 遗留（IndexedDB）类型见 `src/utils/templateStore.ts` 的 `TemplateRecord`（id/name/fileName/size/blob/createdAt/updatedAt），仅死代码使用。

---

## 6. 分析与建议

### 6.1 现状评估

- **上传链路完整性**：✅ 前端 → 后端 → 解析 → 入库 → 预览/编辑全通，错误处理（大小/后缀/魔数）齐全。
- **解析能力**：✅ `parse_template.py` 覆盖全部清单字段，且产出兼容度评分。
- **字段落库程度**：❌ 仅页边距 + 页眉页脚距；字体/字号/标题/页眉页脚/目录/参考文献等解析结果被丢弃。

### 6.2 建议改进点（按优先级）

1. **打通「解析 → 模板字段」映射**：在 `format_task.py::upload_template` 中把 `config["styles"]/["fonts"]/["toc"]/["cover"]` 映射为 `TemplateWritePayload` 的 blocks/styles 写入 v2 模板（`_apply_template_data` 已支持，改动集中）。
2. **保存原始 docx**：为 v2 模板增加原始文件存储（复用 v1 的 style.docx 方案），才能支持「按原模板直接排版」与二次解析。
3. **迁移 v1 遗留模板**：磁盘 6 个旧目录 + DB legacy 行，可用 `migrator.py` 类工具转成 v2 content。
4. **清理死代码**：`UploadTemplate.tsx`、`templateStore.ts`、`template_manager.create_template()`（无调用方）可标记删除或归档。
