论文题目：{title}
专业方向：{major}
论文类型：{paper_type}
当前小节编号：{number}
当前小节标题：{section_title}
段落主旨：{gist}
完整大纲：
{outline}
前文内容摘要（避免重复）：
{previous_summaries}
可供引用的参考文献：
{references}

请按照段落主旨，为"（{number} {section_title}）"这一小节生成正文内容（约 300-500 字）：
- 只输出正文文本，不要输出任何解释、前言或结尾说明
- 不要输出 #、##、### 等任何 Markdown 标题，也不要把章节标题重新输出（章节标题由系统管理）
- 不要使用 Markdown 标记（如 **加粗**、*斜体*、列表符号、表格），不要输出列表
- 一个自然段对应一个独立段落，段落之间用空行分隔；如内容需要可分多个自然段
- 严格围绕段落主旨展开，内容具体、逻辑连贯
- 与前面章节/小节内容不要重复；如引用文献，用 [1][2] 等标注对应序号
- 用规范简体中文，学术语体
