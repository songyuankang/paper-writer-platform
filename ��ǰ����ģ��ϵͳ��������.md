# 当前论文模板系统分析报告

> 生成时间：2026-08-11
> 分析范围：`paper-writer-platform-main`（paper-writer-api + paper-writer-web）
> 目的：梳理现有"论文格式排版模板管理系统"全貌，为后续接入论文导出排版做准备

---

## 一、系统总览

项目内置了一套完整的 **v2 模板系统（Template System）**，采用"**结构化 JSON 模板 + 渲染器 + DOCX 导出器**"架构：

```
前端模板管理页（/templates）
        │  HTTP
        ▼
后端模板 API（/api/templates）
        │
        ▼
TemplateService（业务：解析/复制/默认回退）
        │
        ▼
TemplateRepository（存储：内置=文件，我的=SQLite）
        │
        ▼
TemplateLoader（文件加载）── 内置模板 JSON（basic/、school/）
                        └── 我的模板 content（DB）
        │
        ▼
TemplateValidator（结构校验）
        │
        ▼
spec_converter（论文 spec → 渲染 spec）
        │
        ▼
TemplateRenderer（Template + spec → python-docx Document）
        │
        ▼
DocxExporter（Document → 论文.docx）
```

**核心链路（导出排版）**：
`paper_content（论文数据）→ spec（{meta, sections, references}）→ to_render_spec → TemplateRenderer.render → DocxExporter.export → 论文.docx`

---

## 二、模板相关代码搜索清单

### 2.1 前端（React + TypeScript，`paper-writer-web/src`）

| 文件 | 角色 | 行数 |
|---|---|---|
| `pages/Templates.tsx` | **模板管理页**（列表/筛选/上传/新建/查看/复制/设为默认） | 498 |
| `components/TemplateEditor.tsx` | 模板编辑器（基本信息/页面/编号/目录/参考文献/样式） | 481 |
| `components/StyleFieldEditor.tsx` | 样式字段编辑器（字体/字号/对齐/行距/缩进/段前段后） | 248 |
| `components/UploadTemplate.tsx` | DOCX 模板上传（IndexedDB 本地记忆） | 289 |
| `utils/templateStore.ts` | IndexedDB 模板库（`paper-writer-templates`） | 111 |
| `api/paper.ts`（210-430 行） | 模板 API 客户端（list/get/create/update/duplicate/delete/set-default） | — |
| `components/BodyEditorUniPaper.tsx` | 正文编辑器导出时选模板（template picker） | — |
| `pages/Format.tsx` | 格式处理页（旧版 format 任务，独立体系） | — |
| `App.tsx` | 路由 `/templates` → Templates 页 | — |

### 2.2 后端（Python + FastAPI，`paper-writer-api/app`）

| 文件 | 角色 |
|---|---|
| `api/templates.py` | **模板管理 API**（v2 模板系统全部路由） |
| `models/template.py` | 模板写请求 DTO（结构化入参 + 校验） |
| `formatter/template/models.py` | **模板统一数据模型**（Template/Meta/Block/Style，全系统唯一数据源） |
| `formatter/template/loader.py` | 文件加载层（basic/school JSON、cover.docx） |
| `formatter/template/repository.py` | 存储层（内置=文件扫描，我的=SQLite CRUD） |
| `formatter/template/service.py` | 业务层（resolve/复制/默认回退/校验编排） |
| `formatter/template/validator.py` | 结构校验器 |
| `formatter/template/migrator.py` | schema v1→v2 迁移 |
| `formatter/template/renderer.py` | **渲染器**（Template + spec → python-docx Document） |
| `formatter/template/spec_converter.py` | 论文 spec → 渲染 spec 转换 |
| `formatter/template/render_service.py` | 渲染接入层（resolve → convert → render → export） |
| `formatter/template/exporter.py` | **DOCX 导出器**（Document → .docx 文件） |
| `formatter/template/__init__.py` | 服务单例（get_service） |
| `formatter/service.py` | `format_paper()` 导出入口（新链路优先，异常回退旧构建器） |
| `formatter/format_task.py` / `api/format_task.py` | 旧版格式处理任务（独立体系，保留兼容） |
| `formatter/docx_builder.py` / `style.py` / `toc.py` / `reference.py` | 旧版 docx 构建器（回退路径） |

### 2.3 模板数据/配置目录（`paper-writer-api/app/formatter/templates/`）

```
templates/
├── basic/                          # 内置模板（3 个，随代码分发）
│   ├── course-paper.json           #   课程论文通用模板（basic-course-paper）
│   ├── general-thesis.json         #   学位论文通用模板（basic-general-thesis）
│   └── graduation-thesis.json      #   毕业论文通用模板（basic-graduation-thesis）
├── school/                         # 学校模板目录（新建 school/<slug>/template.json 自动注册）
│   └── ...                         #   （当前为空，机制已就绪）
├── default/                        # 旧版默认格式（template.json + rules.json，兼容用）
└── <uuid>/                         # 旧版上传模板（v1 遗留，标记 legacy）
    ├── style.docx                  #   上传的 Word 模板原件
    ├── template.json               #   v1 元数据（id/name/school_name/major/paper_type）
    ├── rules.json                  #   v1 规则（toc/page/reference/chart/fonts）
    └── template_config.json        #   自动解析结果（页边距/字体/样式）
```

> 当前磁盘上有 6 个旧版上传模板目录（含"郑州经贸"等学校模板），未迁移进 v2 系统（DB 中标记为 legacy / 未注册）。

---

## 三、后端接口清单（v2 模板系统）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/templates` | 模板列表（含 `default_id`、来源/可编辑标记） |
| GET | `/api/templates/{id}` | 模板详情（页面/编号/目录/参考文献/区块/样式摘要） |
| POST | `/api/templates` | 创建"我的模板"（结构化 DTO，可基于 `base_template_id`） |
| PUT | `/api/templates/{id}` | 更新"我的模板"（内置/学校只读，403） |
| POST | `/api/templates/{id}/duplicate` | 复制任意模板为"我的模板" |
| DELETE | `/api/templates/{id}` | 删除"我的模板"（内置/学校 403） |
| POST | `/api/templates/{id}/set-default` | 设置默认模板（全局唯一） |

导出相关（消费模板）：
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/draft/{task_id}/export?template_id=...` | 正文编辑器按模板导出 docx |
| POST | `/api/format/create` + `/start/{id}` | 旧版格式处理任务（独立体系） |

---

## 四、数据库表结构

数据库：SQLite `paper-writer-api/data/history.db`（`app/db.py`，生产可迁移 PostgreSQL）

### 4.1 `format_templates`（模板注册/我的模板存储）

```sql
CREATE TABLE IF NOT EXISTS format_templates (
    id             TEXT PRIMARY KEY,             -- 模板 id（内置=basic-xxx，我的=uuid）
    name           TEXT NOT NULL,                -- 模板名称
    school_name    TEXT NOT NULL DEFAULT '',     -- 学校名称
    major          TEXT NOT NULL DEFAULT '',     -- 专业
    paper_type     TEXT NOT NULL DEFAULT '',     -- 论文类型
    type           TEXT NOT NULL DEFAULT 'mine', -- basic / school / mine
    category       TEXT NOT NULL DEFAULT '',     -- 分类（课程论文/学位论文/毕业论文…）
    description    TEXT NOT NULL DEFAULT '',     -- 描述
    version        INTEGER NOT NULL DEFAULT 1,   -- 版本号（更新 +1）
    schema_version INTEGER NOT NULL DEFAULT 2,   -- 模板 schema 版本（当前 2）
    source         TEXT NOT NULL DEFAULT 'db',   -- 来源（db / file）
    content        TEXT,                         -- 我的模板：完整模板 JSON（v2）
    parent_id      TEXT,                         -- 复制来源模板 id
    is_favorite    INTEGER NOT NULL DEFAULT 0,   -- 收藏
    is_default     INTEGER NOT NULL DEFAULT 0,   -- 默认模板（全局唯一）
    sort_order     INTEGER NOT NULL DEFAULT 0,   -- 排序
    dir            TEXT,                         -- 模板目录（cover.docx 等资源）
    legacy         INTEGER NOT NULL DEFAULT 0,   -- 旧版遗留标记（无 content 的旧上传模板）
    created_at     TEXT NOT NULL,
    updated_at     TEXT NOT NULL
);
```

> 内置模板（basic）也在 DB 注册一行（content 为空），仅用于统一管理"收藏/默认"状态；实际内容以文件为准。
> **我的模板**完整 JSON 存 `content` 列；**内置模板**存文件；**学校模板**存 `school/<slug>/template.json` 文件。

### 4.2 相关表

| 表 | 说明 |
|---|---|
| `generation_records` | 论文生成记录（params 含 template 相关历史参数） |
| `format_tasks` | 旧版格式处理任务（task_id/template_id/status/settings） |
| `revision_versions` | 论文修改版本 |
| `model_configs` | AI 模型配置（生成论文用，与模板无关） |

---

## 五、模板数据结构（v2 Schema，权威定义）

### 5.1 完整 JSON 结构（模板文件/DB content 统一格式）

```json
{
  "schema_version": 2,
  "meta": {
    "id": "basic-course-paper",
    "name": "课程论文通用模板",
    "type": "basic",
    "school": "",
    "school_name": "",
    "major": "",
    "paper_type": "",
    "category": "课程论文",
    "description": "课程论文通用排版：黑体三号一级标题、宋体小四正文…",
    "version": 1,
    "schema_version": 2,
    "builtin": true,
    "source": "db",
    "parent_id": null,
    "is_favorite": false,
    "is_default": false,
    "sort_order": 0,
    "has_cover": false,
    "legacy": false,
    "created_at": "",
    "updated_at": ""
  },
  "page": {
    "size": "A4",
    "orientation": "portrait",
    "margins": { "top_mm": 25, "bottom_mm": 25, "left_mm": 30, "right_mm": 25 },
    "header_distance_mm": 15,
    "footer_distance_mm": 17.5
  },
  "header": { "content": "", "style": { "...TemplateStyle..." } },
  "footer": { "content": "{page}", "style": { "...TemplateStyle..." } },
  "numbering": {
    "enabled": true,
    "h1": "第{chinese}章",
    "h2": "{h1}.{n}",
    "h3": "{h1}.{h2}.{n}",
    "h4": "{h1}.{h2}.{h3}.{n}"
  },
  "blocks": [
    {
      "key": "title_zh",
      "kind": "title_zh",
      "label": "论文标题（中文）",
      "enabled": true,
      "level": null,
      "styles": { "self": { "...TemplateStyle..." } },
      "settings": {}
    }
  ]
}
```

### 5.2 TemplateStyle（样式对象，15 字段，全区块共用）

```json
{
  "font_family": { "east_asia": "宋体", "latin": "Times New Roman" },
  "font_size_pt": 12,
  "bold": false,
  "italic": false,
  "underline": false,
  "alignment": "justify",
  "line_spacing": { "mode": "multiple", "value": 1.5 },
  "space_before_pt": 0,
  "space_after_pt": 0,
  "first_line_indent": { "unit": "chars", "value": 2 },
  "keep_with_next": false,
  "page_break_before": false
}
```

### 5.3 区块（blocks）清单 —— 论文排版要素全覆盖

| key | kind | 排版要素 | 内置默认状态 |
|---|---|---|---|
| `title_zh` | title_zh | 中文标题 | ✅ |
| `title_en` | title_en | 英文标题 | 学位/毕业 ✅ |
| `abstract` | abstract | 摘要（title + content 样式） | ✅ |
| `keywords` | keywords | 关键词 | ✅ |
| `abstract_en` | abstract_en | 英文摘要 | 课程：关 / 学位毕业：开 |
| `keywords_en` | keywords_en | 英文关键词 | 同上 |
| `heading1`~`heading4` | heading | 一~四级标题（level 1-4） | ✅ |
| `body` | paragraph | 正文 | ✅ |
| `figure_caption` | figure_caption | 图标题 | ✅ |
| `table_caption` | table_caption | 表标题 | ✅ |
| `toc` | toc | 目录（settings: include_page_numbers） | ✅ |
| `references` | references | 参考文献（settings: style=gb7714） | ✅ |
| `acknowledgement` | acknowledgement | 致谢 | 课程：关 |
| `appendix` | appendix | 附录 | 课程/毕业：关 |

> `block.styles` 支持多角色：如 `abstract` 块有 `title`（标题样式）+ `content`（正文样式）；`toc` 块有 `title/h1/h2/h3`。未知区块字段保留在 `extra`，**新增区块不改 Schema**。

### 5.4 内置模板差异一览（3 个）

| 模板 | 分类 | 特点 |
|---|---|---|
| `basic-course-paper` 课程论文 | 课程论文 | 中文标题、无英文标题、摘要行距 1.5、正文小四（12pt） |
| `basic-general-thesis` 学位论文 | 学位论文 | 中文+英文标题、摘要行距固定值 20pt、参考文献 16pt 固定行距 |
| `basic-graduation-thesis` 毕业论文 | 毕业论文 | 中文+英文标题、一级标题居中、致谢开 |

### 5.5 TypeScript interface（前端，`api/paper.ts`）

```ts
export interface TemplateSummary {
  id: string; name: string; description: string; category: string;
  paper_type: string; source: TemplateSource; type: string;
  school_name: string; major: string; version: number;
  created_at: string; updated_at: string; is_default: boolean;
  is_favorite: boolean; has_cover: boolean; legacy: boolean; editable: boolean;
}

export interface TemplateStyleInput {
  font_family: { east_asia: string; latin: string };
  font_size_pt: number; bold: boolean; italic: boolean; underline: boolean;
  alignment: "left"|"center"|"right"|"justify";
  line_spacing: { mode: "multiple"|"exact"|"at_least"; value: number };
  space_before_pt: number; space_after_pt: number;
  first_line_indent: { unit: "chars"|"pt"; value: number };
  keep_with_next: boolean; page_break_before: boolean;
}

export interface TemplateBlockInput {
  key: string; kind: string; label: string; enabled: boolean;
  level?: number | null;
  styles: Record<string, TemplateStyleInput>;
  settings: Record<string, unknown>;
}

export interface TemplatePageInput {
  size: TemplatePageSize;            // "A3"|"A4"|"A5"|"B4"|"B5"|"Letter"|"Legal"|"Tabloid"
  orientation: "portrait"|"landscape";
  margins: { top_mm: number; bottom_mm: number; left_mm: number; right_mm: number };
  header_distance_mm?: number | null; footer_distance_mm?: number | null;
}

export interface TemplateWritePayload {
  base_template_id?: string | null;
  name: string; description: string; category: string;
  paper_type: string; school_name: string; major: string;
  page?: TemplatePageInput | null;
  header?: TemplateHeaderFooterInput | null;
  footer?: TemplateHeaderFooterInput | null;
  numbering?: TemplateNumberingInput | null;
  toc?: TemplateTocInput | null;
  reference_style?: "gb7714"|"apa"|"mla"|"chicago" | null;
  blocks?: TemplateBlockInput[] | null;
}

export interface TemplateDetail extends TemplateSummary {
  page: TemplatePageInput;
  numbering: TemplateNumberingInput;
  toc: { enabled: boolean; include_page_numbers: boolean };
  reference_style: TemplateReferenceStyle;
  header: TemplateHeaderFooterInput;
  footer: TemplateHeaderFooterInput;
  blocks: TemplateBlockInput[];
  styles: {
    title?: TemplateStyleSummary | null;
    heading1?: TemplateStyleSummary | null;
    body?: TemplateStyleSummary | null;
    references?: TemplateStyleSummary | null;
  };
}
```

---

## 六、字段覆盖对照（对照需求逐项核对）

| 需求字段 | 是否覆盖 | 存储位置 |
|---|---|---|
| 模板名称 | ✅ | `meta.name` |
| 学校 | ✅ | `meta.school_name` / `meta.school` |
| 论文类型 | ✅ | `meta.paper_type` |
| 页面设置（纸张/方向/页边距） | ✅ | `page.size/orientation/margins` |
| 页眉/页脚（内容+样式） | ✅ | `header.content/style`、`footer.content/style`（支持 `{page}` 页码占位） |
| 字体（中/西文） | ✅ | `style.font_family.east_asia/latin` |
| 字号 | ✅ | `style.font_size_pt` |
| 加粗 | ✅ | `style.bold` |
| 对齐 | ✅ | `style.alignment`（左/中/右/两端） |
| 首行缩进 | ✅ | `style.first_line_indent`（字符/磅两种单位） |
| 行间距 | ✅ | `style.line_spacing`（倍数/固定值/最小值） |
| 段前/段后 | ✅ | `style.space_before_pt / space_after_pt` |
| 一级/二级/三级标题 | ✅ | `blocks[heading1/2/3/4]`（含四级） |
| 摘要/关键词 | ✅ | `blocks[abstract/keywords]`（中英文） |
| 目录 | ✅ | `blocks[toc]`（页码开关） |
| 正文 | ✅ | `blocks[body]` |
| 参考文献 | ✅ | `blocks[references]`（gb7714/apa/mla/chicago） |
| 其他（图题注/表题注/致谢/附录） | ✅ 扩展支持 | `blocks[figure_caption/table_caption/acknowledgement/appendix]` |

**额外能力（超出需求清单）**：斜体 `italic`、下划线 `underline`、与下段同页 `keep_with_next`、段前分页 `page_break_before`、标题编号模板 `numbering`（`第{chinese}章`、`{h1}.{n}` 等占位符）。

---

## 七、分析结论

### 7.1 模板配置是否完整？ —— **基本完整**

- 论文排版的**全部要素**（页面/页边距/字体/字号/对齐/行距/缩进/段距/标题层级/摘要/关键词/目录/参考文献/页眉页脚）都有结构化字段；
- 3 个内置模板开箱即用，schema 版本 v2，字段带默认值兜底，缺字段不会崩溃；
- 渲染链路（spec 转换 → 渲染 → DOCX 导出）已完整跑通，且有"异常回退旧构建器"保护。

### 7.2 是否支持不同学校模板？ —— **✅ 支持（机制就绪，数据待迁移）**

- 目录机制：`templates/school/<slug>/template.json` **新建即自动注册，无需改代码**；
- `meta.school_name` 字段原生支持学校区分；
- 旧版上传的 6 个学校模板（含"郑州经贸"等）仍在磁盘上，但**未迁移到 v2 格式**（缺完整 blocks，仅 v1 元数据 + rules.json），当前在列表中不可见。

### 7.3 是否支持动态新增模板？ —— **✅ 支持**

- 前端：`/templates` 页"新建模板 / 上传 DOCX / 复制内置"，`TemplateEditor` 全字段可视化编辑；
- 后端：`POST /api/templates`（结构化 DTO）+ `Validator` 校验 + 自动建目录；
- 我的模板存 DB `content` 列，版本号自动 +1；
- 内置模板只读保护，需"复制为我的模板"再编辑。

### 7.4 是否能直接用于 docx 导出？ —— **✅ 已打通，但存在双链路**

- **新链路（v2）**：`render_with_template(template_id) → TemplateRenderer（python-docx）→ DocxExporter → 论文.docx`，正文编辑器导出已接入（`/api/draft/{task_id}/export?template_id=...`）；
- **旧链路**：`docx_builder.build_docx`（无 template_id 时使用，含默认/学校模板旧路径）；
- 注意：**当前 `POST /api/generate` 生成主流程已移除 template_id 参数**（此前清理），默认走旧构建器/默认模板；模板选择目前只在**正文编辑器导出**与**格式处理页**生效。若要让"生成 → 导出"全程可选手动模板，需要重新把 template_id 接入生成请求或导出环节。

### 7.5 缺少哪些字段 / 可改进点？

| 缺口 | 说明 | 影响 |
|---|---|---|
| **封面/封底** | `meta.has_cover` 有标记位，但 v2 模板 JSON 无封面内容结构（旧版靠 cover.docx 母版） | 学校论文封面（校名/题目/姓名等）无法纯 JSON 表达 |
| **单页/分节设置** | 无"目录独立页码（罗马数字）"“正文重新起页”“奇偶页不同页眉”等分节控制 | 学位论文常见"摘要用罗马数字页码、正文从 1 重排"无法表达 |
| **表格样式** | 只有 `table_caption`（表标题），无表格线框/表头底纹/单元格字体 | 复杂表格排版缺失 |
| **页眉分节** | `header.content` 单一字符串，无"封面无页眉、正文页眉不同" | 学校模板常见需求 |
| **字体嵌入/缺失兜底** | 仅记录字体名，未处理字体缺失 | 导出到未装字体的机器会回退 |
| **旧模板迁移** | 6 个旧版上传模板仍是 v1（无 blocks） | 需要写迁移器（已有 `migrator.py`，v1→v2 通道存在） |
| **默认模板回退链** | DB 无 is_default 时回退到链首（basic 优先），无"按论文类型自动匹配模板" | 生成毕业论文时不会自动选"毕业论文模板" |
| **封面字段** | 无"封面页显示哪些字段（题目/作者/学号/导师/日期）"的配置 | 学校封面需要 |

---

## 八、后续接入"论文导出排版"的建议

1. **优先复用 v2 新链路**：`render_with_template(template_id, task_dir, spec, paper_info)` 已是完整闭环，接入点清晰（spec_converter 是唯一入口）。
2. **恢复生成流程的模板选择**：若要求"提交生成任务时选模板"，需在 `POST /api/generate` 重新加入 `template_id` 参数并传给 `format_paper`（新链路优先，异常回退）。
3. **学校模板迁移**：用现有 `TemplateMigrator` 把 6 个旧版模板（style.docx + rules.json + template_config.json）转成 v2（生成 blocks），放入 `school/<slug>/`。
4. **扩展缺口字段**（如需学校级精度）：
   - 封面结构（新增 `cover` 区块 kind）；
   - 分节/页码体系（新增 `section_settings`）；
   - 表格样式（新增 `table` 区块 kind）。
5. **按论文类型自动选模板**：在 `TemplateService.resolve` 增加 `paper_type → 模板` 匹配规则。
